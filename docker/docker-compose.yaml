version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: outweb-database
    ports:
     - "15432:5432"
    environment:
     - POSTGRES_USER=outweb
     - POSTGRES_PASSWORD=darkweb
     - POSTGRES_DB=outweb

  php-fpm:
    container_name: php-fpm
    build:
      context: ./php-fpm
    depends_on:
      - db
    environment:
      - APP_ENV=dev
      - APP_SECRET=1fb2b20db3894c3c3265d7e8feac1208
      - DATABASE_URL="postgresql://outweb:darkweb@127.0.0.1:15432/outweb?serverVersion=12&charset=utf8"
    volumes:
      - ./../:/var/www

  nginx:
    container_name: nginx
    build:
      context: ./nginx
    volumes:
      - ./../:/var/www
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/sites/:/etc/nginx/sites-available
      - ./nginx/conf.d/:/etc/nginx/conf.d
      - ./logs:/var/log
    depends_on:
      - php-fpm
    ports:
      - "9999:80"
      - "443:443"
  adminer:
    image: adminer:latest
    ports:
     - "9001:8080"
    depends_on:
     - db
