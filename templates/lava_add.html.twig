<html xmlns="http://www.w3.org/1999/xhtml" >
    <head lang="en">
        {{ include('fragments/_head.html.twig') }}
    </head>
    <body> {% block body %}
        <header>
            {{ include('fragments/_top_navi.html.twig') }}   
        </header>
        <div class="container">
            <div class="row">
                {{ form_start(form) }}
                <div class="col-md-2 col-sm-8">{{ form_widget(form.kaytava) }}</div>
                <div class="col-md-2 col-sm-8">{{ form_widget(form.vali) }}</div>
                <div class="col-md-2 col-sm-8">{{ form_widget(form.taso) }}</div>
                <div class="col-md-2 col-sm-8">{{ form_widget(form.reuna) }}</div>
                <div class="col-md-1 col-sm-4">{{ form_widget(form.add) }}</div>
                    {{ form_end(form) }}
            </div>
            {{ text }}
            
        </div>
            {% endblock %}
    </body>
